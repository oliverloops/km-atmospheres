---
export interface Props {
	title: string;
    type?: 'hero' | 'form';
}

const { title, type } = Astro.props;
---

<button class={type === 'form' ? 'black-btn' : 'hero-btn'}>
    {title}
</button>

<style>
    .hero-btn {
        width: 220px;
        height: 50px;
        background: rgba(255, 255, 255, 0.4);
        border: 2px solid #FFFFFF;
        border-radius: 50px;
        font-size: 18;
        display: flex;
        justify-content: center;
        align-items: center;
        margin-top: 20px;
        font-family: 'PPTelegraf-UltraBold', sans-serif;
        font-size: 0.9rem;
        color: #fff;
        cursor: pointer;
    }
    :hover{
        background: rgba(255, 255, 255, 0.6);
    }
    .black-btn {
        width: 100%;
        height: 58px;
        background: #000000;
        border-radius: 7px;
        display: flex;
        justify-content: center;
        align-items: center;
        color: #fff;
        cursor: pointer;
        font-size: 1.25rem;
        font-family: 'PPTelegraf-UltraBold', sans-serif;
    }

    // Media Queries
    @media (min-width: 280px) and (max-width: 767px){
        .hero-btn {
            width: 170px;
            height: 50px;
            font-size: 16;
            margin-top: 50px;
        }
    }
</style>